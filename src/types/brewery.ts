export type Brewery = {
  street: string | null,
  postal_code: string | null,
  city: string | null,
  state: string | null,
  country: string | null,
  phone: string | null,
  name: string | null,
  website_url: string | null,
  latitude: string | null,
  longitude: string | null,
  brewery_type: string,
  id: string | null,
}